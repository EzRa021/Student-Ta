# ============================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================================
# LOCAL DEVELOPMENT: Copy this to .env and fill in local values
# PRODUCTION: Set these as environment variables on Render.com

# Spring Profile
SPRING_PROFILE=local

# Database Configuration
# For MySQL locally or on Render
DB_URL=jdbc:mysql://localhost:3306/lms_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USER=lms_user
DB_PASS=secure_password_here

# ============================================================
# JWT Configuration (REQUIRED)
# Generate with: openssl rand -base64 32
# Minimum 32 characters for security
# ============================================================
JWT_SECRET=your_jwt_secret_here_must_be_32_chars_minimum_xxxxxxxxxxxxxxxxxx
JWT_EXPIRY=900000

# JWT Refresh Secret (REQUIRED)
# Different from JWT_SECRET for security
REFRESH_SECRET=your_refresh_secret_here_must_be_32_chars_minimum_xxxxxxxx
REFRESH_EXPIRY=604800000

# ============================================================
# AES Encryption Key (32 chars for AES-256)
# ============================================================
AES_KEY=0123456789ABCDEF0123456789ABCDEF

# ============================================================
# Server Configuration
# ============================================================
SERVER_PORT=8080

# ============================================================
# RENDER.COM DEPLOYMENT NOTES
# ============================================================
# When deploying to Render:
# 1. Set SPRING_PROFILE=prod
# 2. All JWT_SECRET values must be set as Render environment variables
# 3. Database credentials must be set as Render environment variables
# 4. This .env file should NOT be deployed (add to .gitignore)
# 5. Generate strong secrets using: openssl rand -base64 32
